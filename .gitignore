# Python 垃圾文件
__pycache__/
*.py[cod]

# VS Code 个人配置
.vscode/

# Node/JS 依赖包
node_modules/

# 系统垃圾文件
.DS_Store
Thumbs.db

# 错误日志
*.log
npm-debug.log*

# 这里的逻辑是：
# .css, .html, .js, .py, .obj (若是模型) 都会被自动包含，因为它们不在这个名单里。
# End of ignore list

# ==========================
# 游戏存档区域 (Game Saves)
# ==========================
# 1. 忽略 saves 文件夹内的【所有】文件 (包含 slot_1.json 等)
saves/*

# 2. 【重要】例外规则：不要忽略 .gitkeep 文件
# 这样 Git 才会把 saves 文件夹本身传上去，但不会传里面的存档
!saves/.gitkeep
